package businessLayer;

import dto.User;

public interface CreditProcessor {
	
	float checkSerial(String serial);
	
	float updateCredit(float value, User user);
	
	void addSerial(String serial,int value);

}
